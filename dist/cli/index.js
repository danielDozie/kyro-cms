#!/usr/bin/env node
import'../chunk-5J6HXUF6.js';import {Command}from'commander';import A,{readFileSync,existsSync,mkdirSync,writeFileSync}from'fs';import E,{dirname,join,resolve}from'path';import {fileURLToPath}from'url';import {glob}from'glob';import {execSync}from'child_process';import ie from'prompts';import Y from'crypto';import a from'chalk';import C from'ora';function D(e){switch(e.type){case "text":case "email":case "password":case "textarea":case "color":case "code":case "markdown":case "date":case "richtext":case "json":return e.hasMany?"string[]":"string";case "number":return e.hasMany?"number[]":"number";case "checkbox":return "boolean";case "select":case "radio":if(e.options){let t=e.options.map(r=>`'${r.value}'`).join(" | ");return e.hasMany?`(${t})[]`:t}return e.hasMany?"string[]":"string";case "relationship":if(Array.isArray(e.relationTo)){if(e.relationTo.includes("*"))return e.hasMany?"any[]":"any";let t=e.relationTo.map(r=>`${w(r)} | string`).join(" | ");return e.hasMany?`(${t})[]`:`(${t})`}let o=e.relationTo||"unknown";return o==="*"?e.hasMany?"any[]":"any":e.hasMany?`(${w(o)} | string)[]`:`${w(o)} | string`;case "upload":return e.hasMany?"(Media | string)[]":"Media | string";case "array":return e.fields&&e.fields.length>0?`Array<{
${e.fields.filter(r=>r.name).map(r=>{let n=D(r);return `    ${r.name}${r.required?"":"?"}: ${n};`}).join(`
`)}
  }>`:"Record<string, any>[]";case "group":return e.fields&&e.fields.length>0?`{
${e.fields.filter(r=>r.name).map(r=>{let n=D(r);return `  ${r.name}${r.required?"":"?"}: ${n};`}).join(`
`)}
}`:"Record<string, any>";case "blocks":return "Block[]";default:return "any"}}function w(e){return e.charAt(0).toUpperCase()+e.slice(1).replace(/-([a-z])/g,(o,t)=>t.toUpperCase())}function K(e){return e.replace(/-([a-z])/g,(o,t)=>t.toUpperCase())}function oe(e){let o=w(e.slug),t=e.fields.filter(r=>r.name).map(r=>{let n=D(r);return `  ${r.name}${r.required?"":"?"}: ${n};`});return t.unshift("  id: string;"),t.push("  createdAt: string;"),t.push("  updatedAt: string;"),`export interface ${o} {
${t.join(`
`)}
}`}function G(e){let o=e.map(s=>oe(s)),t=`// ============================================================================
// Auto-generated by kyro generate
// ============================================================================

import type { RichTextBlock } from '@kyro-cms/core';

export interface Media {
  id: string;
  url: string;
  filename: string;
  mimeType: string;
  filesize: number;
  width?: number;
  height?: number;
  createdAt: string;
  updatedAt: string;
}

export interface Block {
  blockType: string;
  [key: string]: any;
}

`,r=o.join(`

`),n=`

// ============================================================================
// Query Types
// ============================================================================

export interface KyroWhereClause {
  [field: string]: {
    equals?: any;
    not_equals?: any;
    in?: any[];
    not_in?: any[];
    greater_than?: number;
    greater_than_equal?: number;
    less_than?: number;
    less_than_equal?: number;
    like?: string;
    exists?: boolean;
  };
}

export interface KyroFindArgs {
  where?: KyroWhereClause;
  sort?: string;
  limit?: number;
  page?: number;
  depth?: number;
  select?: string[];
}

export interface KyroFindResult<T> {
  docs: T[];
  totalDocs: number;
  limit: number;
  totalPages: number;
  page: number;
  pagingCounter: number;
  hasPrevPage: boolean;
  hasNextPage: boolean;
  prevPage: number | null;
  nextPage: number | null;
}

export interface KyroCreateResult<T> {
  doc: T;
  message?: string;
}

export interface KyroUpdateResult<T> {
  doc: T;
  message?: string;
}

export interface KyroDeleteResult<T> {
  doc: T;
  message: string;
}

// ============================================================================
// Collection Map
// ============================================================================

export interface KyroCollections {
${e.map(s=>`  ${K(s.slug)}: ${w(s.slug)};`).join(`
`)}
}

// ============================================================================
// Typed Client
// ============================================================================

export interface KyroTypedClient<C extends keyof KyroCollections> {
  find: (args?: KyroFindArgs) => Promise<KyroFindResult<KyroCollections[C]>>;
  findByID: (id: string, args?: { depth?: number; select?: string[] }) => Promise<KyroCollections[C]>;
  create: (data: Partial<KyroCollections[C]>) => Promise<KyroCreateResult<KyroCollections[C]>>;
  update: (id: string, data: Partial<KyroCollections[C]>) => Promise<KyroUpdateResult<KyroCollections[C]>>;
  delete: (id: string) => Promise<KyroDeleteResult<KyroCollections[C]>>;
  count: (args?: { where?: KyroWhereClause }) => Promise<{ totalDocs: number }>;
}

export interface KyroClient {
  ${e.map(s=>`  ${K(s.slug)}: KyroTypedClient<'${s.slug}'>;`).join(`
`)}
}

export function createClient(config: { url: string; token?: string }): KyroClient {
  // Client implementation would be here
  throw new Error('Not implemented');
}
`;return t+r+n}async function ne(e,o){let t=`${e.replace(/\/+$/,"")}/api/__schema`,r=await fetch(t,{headers:{Authorization:`ApiKey ${o}`}});if(!r.ok){let n=await r.text();throw new Error(`Schema fetch failed (${r.status}): ${n}`)}return r.json()}function se(e){let o=`// ============================================================================
// Auto-generated by kyro generate --api
// ============================================================================

import type { RichTextBlock } from '@kyro-cms/core';

export interface Media {
  id: string;
  url: string;
  filename: string;
  mimeType: string;
  filesize: number;
  width?: number;
  height?: number;
  createdAt: string;
  updatedAt: string;
}

export interface Block {
  blockType: string;
  [key: string]: any;
}

`;for(let t of e){let r=w(t.slug),n=t.fields.filter(s=>s.name).map(s=>{let l=D(s);return `  ${s.name}${s.required?"":"?"}: ${l};`});n.unshift("  id: string;"),n.push("  createdAt: string;"),n.push("  updatedAt: string;"),o+=`export interface ${r} {
${n.join(`
`)}
}

`;}return o+=`// ============================================================================
// Typed REST Client
// ============================================================================

export interface KyroClientOptions {
  url: string;
  apiKey: string;
  fetch?: typeof globalThis.fetch;
}

export function createKyroClient(opts: KyroClientOptions) {
  const base = opts.url.replace(/\\/+$/, "");
  const http = opts.fetch || globalThis.fetch;

  async function request<T>(path: string, init?: RequestInit): Promise<T> {
    const res = await http(\`\${base}/api\${path}\`, {
      headers: {
        "Authorization": \`ApiKey \${opts.apiKey}\`,
        "Content-Type": "application/json",
        ...init?.headers,
      },
      ...init,
    });
    if (!res.ok) {
      const body = await res.text();
      throw new Error(\`Kyro API error \${res.status}: \${body}\`);
    }
    return res.json();
  }

  function qs(params: Record<string, any>): string {
    const s = new URLSearchParams();
    for (const [k, v] of Object.entries(params)) {
      if (v !== undefined && v !== null) s.set(k, typeof v === "object" ? JSON.stringify(v) : String(v));
    }
    return s.toString();
  }

  return {
${e.map(t=>{let r=w(t.slug);return `    ${K(t.slug)}: {
      find: (args?: { where?: Record<string, any>; sort?: string; limit?: number; page?: number; draft?: boolean }) =>
        request<{ docs: ${r}[]; totalDocs: number; page: number; totalPages: number; hasNextPage: boolean; hasPrevPage: boolean }>(\`/${t.slug}\${args ? "?" + qs(args) : ""}\`),
      findByID: (id: string, args?: { draft?: boolean }) =>
        request<${r}>(\`/${t.slug}/\${id}\${args ? "?" + qs(args) : ""}\`),
      findBySlug: (slug: string, args?: { draft?: boolean }) =>
        request<{ docs: ${r}[] }>(\`/${t.slug}?where=\${encodeURIComponent(JSON.stringify({ slug: { equals: slug } }))}\`),
      create: (data: Partial<${r}>) =>
        request<{ doc: ${r}; message?: string }>(\`/${t.slug}\`, { method: "POST", body: JSON.stringify(data) }),
      update: (id: string, data: Partial<${r}>, args?: { baseUpdatedAt?: string }) =>
        request<{ doc: ${r}; message?: string }>(\`/${t.slug}/\${id}\`, { method: "PATCH", body: JSON.stringify({ ...data, ...args }) }),
      delete: (id: string) =>
        request<{ doc: ${r}; message: string }>(\`/${t.slug}/\${id}\`, { method: "DELETE" }),
      count: (args?: { where?: Record<string, any> }) =>
        request<{ totalDocs: number }>(\`/${t.slug}/count\${args ? "?" + qs(args) : ""}\`),
    },`}).join(`
`)}
  };
}
`,o}function Q(){return new Command("generate").description("Generate TypeScript types from collection configs").option("-c, --config <path>","Path to config file or directory","./src/kyro").option("-o, --output <path>","Output file path","./src/types/kyro.d.ts").option("-w, --watch","Watch for changes and regenerate").option("--url <url>","URL of a running Kyro CMS instance (enables remote mode)").option("--api-key <key>","API key for authentication (required with --url)").action(async o=>{if(o.url){o.url||(console.error("\u274C --url is required when using --api"),process.exit(1)),o.apiKey||(console.error("\u274C --api-key is required when using --api"),process.exit(1));let n=resolve(o.output);try{let s=await ne(o.url,o.apiKey),l=se(s),m=join(n,"..");existsSync(m)||mkdirSync(m,{recursive:!0}),writeFileSync(n,l);}catch(s){console.error("\u274C Error:",s.message),process.exit(1);}return}let t=resolve(o.config),r=resolve(o.output);try{let n=await glob(`${t}/**/*.{ts,js}`);n.length===0&&(console.error("\u274C No config files found at:",t),process.exit(1));let s=[];for(let d of n)try{let p=await import(d),h=Object.values(p).filter(g=>g&&g.slug&&g.fields);s.push(...h);}catch{console.warn(`\u26A0\uFE0F  Could not parse: ${d}`);}s.length===0&&(console.error("\u274C No valid collection configs found"),process.exit(1));let l=G(s),m=join(r,"..");if(existsSync(m)||mkdirSync(m,{recursive:!0}),writeFileSync(r,l),o.watch){let{watch:d}=await import('fs');d(t,{recursive:!0},async(p,h)=>{let g=[];for(let x of n)try{let M=await import(x+`?t=${Date.now()}`),O=Object.values(M).filter(u=>u&&u.slug&&u.fields);g.push(...O);}catch{}let S=G(g);writeFileSync(r,S);});}}catch(n){console.error("\u274C Error:",n.message),process.exit(1);}})}function ce(e=3){return Y.randomBytes(e).toString("hex")}function le(){return Y.randomBytes(12).toString("base64").replace(/[^a-zA-Z0-9]/g,"").slice(0,16)}function J(){let e=new Command("deploy").description("Deploy Kyro CMS");return e.command("cloudflare").description("Deploy to Cloudflare Workers with Assets").option("-d, --database <type>","Database type (d1|postgres)").option("-u, --database-url <url>","PostgreSQL connection string").option("-n, --name <name>","Cloudflare project name").option("-b, --r2-bucket <name>","R2 storage bucket name").option("-e, --email <email>","Initial Super Admin email").option("-p, --password <password>","Initial Super Admin password").option("-y, --non-interactive","Skip all prompts and use defaults").option("-q, --quiet","Run without prompts or spinners").option("-j, --json","Emit a final JSON line with deploy results (for programmatic consumers)").action(async o=>{console.log(`
  ${a.cyan.bold("\u2726 Kyro CMS")} ${a.dim("\u2014 Cloudflare Deployment")}
`);let t=ce(),r=le(),{database:n,databaseUrl:s,name:l,r2Bucket:m,email:d,password:p,nonInteractive:h,quiet:g,json:S}=o;g&&(h=true);let x=`kyro-postgres-hd-${t}`;if(s&&!n&&(n="postgres"),!h&&process.stdout.isTTY){let c=[];n||c.push({type:"select",name:"database",message:"Select database infrastructure:",choices:[{title:"Cloudflare D1 (Native Serverless SQLite, auto-provisioned)",value:"d1"},{title:"PostgreSQL (External DB via Cloudflare Hyperdrive)",value:"postgres"}],initial:0}),n==="postgres"&&!s&&c.push({type:"text",name:"databaseUrl",message:"PostgreSQL Connection URL:"}),l||c.push({type:"text",name:"name",message:"Cloudflare Project Name:",initial:`kyro-app-${t}`}),m||c.push({type:"text",name:"r2Bucket",message:"Cloudflare R2 Bucket Name:",initial:`kyro-media-${t}`}),d||c.push({type:"text",name:"email",message:"Initial Super Admin Email:",initial:"admin@kyro-cms.com"}),p||c.push({type:"text",name:"password",message:"Initial Super Admin Password (leave blank to auto-generate):"});let i=await ie(c);i.database&&(n=i.database),i.databaseUrl&&(s=i.databaseUrl),i.name&&(l=i.name),i.r2Bucket&&(m=i.r2Bucket),i.email&&(d=i.email),i.password&&(p=i.password);}l=l||`kyro-app-${t}`,m=m||`kyro-media-${t}`,d=d||"admin@kyro-cms.com",p=p||r,n=n||"d1",console.log(a.bgGray.black.bold(`
 Deployment Plan `)),console.log(`  ${a.dim("\u251C\u2500")} Hosting   : ${a.cyan("Cloudflare Workers with Assets")}`),console.log(`  ${a.dim("\u251C\u2500")} Database  : ${a.cyan(n==="d1"?"Cloudflare D1 (Native)":"PostgreSQL (Hyperdrive)")}`),console.log(`  ${a.dim("\u251C\u2500")} Project   : ${a.cyan(l)}`),console.log(`  ${a.dim("\u251C\u2500")} R2 Bucket : ${a.cyan(m)}`),console.log(`  ${a.dim("\u2514\u2500")} Admin     : ${a.cyan(d)}
`);let O=A.existsSync(E.join(process.cwd(),"pnpm-lock.yaml"))?"pnpm":"npm",u="npx wrangler";try{execSync(`${u} whoami`,{stdio:"ignore"}),console.log(`  ${a.green("\u2714")} Cloudflare Wrangler authenticated`);}catch{console.log(`  ${a.red("\u2716")} Cloudflare Wrangler authentication required.`),console.log(`    Run ${a.bold(`${u} login`)} or set the ${a.bold("CLOUDFLARE_API_TOKEN")} env var.`),process.exit(1);}let v="",k="",$=g?{isSilent:true}:{};if(n==="postgres"){s||(console.log(`  ${a.red("\u2716")} PostgreSQL mode requires a database URL.`),process.exit(1));let c=C({text:"Checking existing Hyperdrive resources...",...$}).start();try{let i=execSync(`${u} hyperdrive list --json`,{stdio:"pipe"}).toString(),f=JSON.parse(i.slice(i.indexOf("["))).find(B=>B.name===x);f&&(k=f.id||f.uuid),c.succeed("Hyperdrive check complete");}catch{c.fail("Failed to list Hyperdrive resources");}if(!k){let i=C({text:"Creating Hyperdrive...",...$}).start();try{let T=execSync(`${u} hyperdrive create "${x}" --connection-string="${s}" --json`,{stdio:"pipe"}).toString(),f=JSON.parse(T.slice(T.indexOf("{")));k=f.id||f.uuid,i.succeed("Hyperdrive created");}catch{i.fail("Failed to create Hyperdrive");}}console.log(`  ${a.green("\u2714")} Hyperdrive resource ready (${x})`);}else {let c=`${l}-d1`,i=C({text:"Creating D1 database...",...$}).start();try{let f=execSync(`${u} d1 create "${c}"`,{stdio:"pipe"}).toString().match(/([a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12})/i);f&&(v=f[1]),i.succeed("D1 database created");}catch{try{let f=execSync(`${u} d1 list --json`,{stdio:"pipe"}).toString(),N=JSON.parse(f).find(ee=>ee.name===c);N&&(v=N.id||N.uuid),i.succeed("Existing D1 database found");}catch{i.fail("Failed to create/find D1 database");}}v||(console.log(`  ${a.red("\u2716")} Failed to create/find D1 database '${c}'. (D1 database limit may be reached).`),process.exit(1));}let U=C({text:"Creating R2 bucket...",...$}).start();try{execSync(`${u} r2 bucket create "${m}"`,{stdio:"ignore"}),U.succeed("R2 bucket created");}catch{U.fail("Failed to create R2 bucket");}try{execSync(`echo "y" | ${u} r2 bucket dev-url enable "${m}"`,{stdio:"ignore"});}catch{}let V=C({text:"Generating wrangler.toml...",...$}).start(),P=`name = "${l}"
compatibility_date = "2026-07-31"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "dist/client"
binding = "ASSETS"
`;n==="postgres"?P+=`
[[hyperdrive]]
binding = "HYPERDRIVE"
id = "${k}"
`:P+=`
[[d1_databases]]
binding = "DB"
database_name = "${l}-d1"
database_id = "${v}"
`,P+=`
[[r2_buckets]]
binding = "STORAGE_BUCKET"
bucket_name = "${m}"
`,A.writeFileSync(E.join(process.cwd(),"wrangler.toml"),P,"utf8"),V.succeed("wrangler.toml generated");let Z=`import bcrypt from 'bcryptjs'; console.log(bcrypt.hashSync('${p}', 10));`,j="";try{j=execSync(`node -e "${Z}"`,{stdio:"pipe"}).toString().trim();}catch{}if(n==="postgres"){try{execSync(`DATABASE_URL="${s}" npx drizzle-kit push --force`,{stdio:"ignore"});}catch{}let c=`
          import postgres from 'postgres';
          import bcrypt from 'bcryptjs';
          const sql = postgres(process.env.DATABASE_URL || '${s}');
          async function bootstrap() {
            try {
              await sql\`CREATE TABLE IF NOT EXISTS users (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), email VARCHAR(255) NOT NULL, password_hash VARCHAR(255), role VARCHAR(50) DEFAULT 'customer', email_verified BOOLEAN DEFAULT true, created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW())\`;
              await sql\`CREATE UNIQUE INDEX IF NOT EXISTS users_email_idx ON users (email)\`;
              const existing = await sql\`SELECT id FROM users WHERE email = '${d}'\`;
              if (existing.length === 0) {
                const hash = bcrypt.hashSync('${p}', 10);
                await sql\`INSERT INTO users (email, password_hash, role, email_verified) VALUES ('${d}', \\\${hash}, 'super_admin', true)\`;
              }
            } catch (e) {
            } finally { await sql.end(); }
          }
          bootstrap();
        `;try{execSync(`node -e "${c.replace(/\n/g," ")}"`,{stdio:"pipe"});}catch{}console.log(`  ${a.green("\u2714")} PostgreSQL schema migrated & Super Admin seeded`);}else {let c=`
          CREATE TABLE IF NOT EXISTS users (
            id TEXT PRIMARY KEY,
            email TEXT UNIQUE NOT NULL,
            password_hash TEXT,
            role TEXT DEFAULT 'customer',
            email_verified INTEGER DEFAULT 1,
            created_at TEXT DEFAULT CURRENT_TIMESTAMP,
            updated_at TEXT DEFAULT CURRENT_TIMESTAMP
          );
          INSERT OR IGNORE INTO users (id, email, password_hash, role, email_verified)
          VALUES ('admin-super-1', '${d}', '${j}', 'super_admin', 1);
        `;try{execSync(`${u} d1 execute "${l}-d1" --remote --command="${c.replace(/\n/g," ")}"`,{stdio:"ignore"}),console.log(`  ${a.green("\u2714")} D1 schema migrated & Super Admin seeded`);}catch{}}let q=C({text:"Building Astro project...",...$}).start();try{execSync(`${O} run build`,{stdio:"inherit"});let c=E.join(process.cwd(),"wrangler.toml"),i=A.readFileSync(c,"utf8");i.includes('main = "dist/server/entry.mjs"')||A.writeFileSync(c,`main = "dist/server/entry.mjs"
`+i,"utf8"),q.succeed("Build complete");}catch{q.fail("Build failed"),console.log(`
  ${a.red("\u2716")} Build failed. Inspect output above.`),process.exit(1);}let F=C({text:"Deploying to Cloudflare Workers...",...$}).start();A.existsSync(E.join(process.cwd(),".wrangler"))&&A.rmSync(E.join(process.cwd(),".wrangler"),{recursive:true,force:true});let _="";try{let c=execSync(`${u} deploy`,{stdio:"pipe"}).toString();process.stdout.write(c);let i=c.match(/https:\/\/[a-zA-Z0-9._-]+\.workers\.dev/);i&&(_=i[0]),F.succeed("Deployment successful"),console.log(`
  ${a.green.bold("\u{1F389} Deployment Successful!")}
`),console.log(`  ${a.bold("Super Admin Credentials")}`),console.log(`  ${a.dim("\u251C\u2500")} ${a.bold("Email   :")} ${a.cyan(d)}`),console.log(`  ${a.dim("\u2514\u2500")} ${a.bold("Password:")} ${a.yellow.bold(p)}`),_&&console.log(`  ${a.dim("\u2514\u2500")} ${a.bold("Live URL:")} ${a.cyan(_)}`),console.log(`
  ${a.dim("\u26A0\uFE0F  Save these credentials. Password won't be shown again.")}
`),S&&process.stdout.write(JSON.stringify({ok:!0,liveUrl:_,adminEmail:d,adminPassword:p})+`
`);}catch{F.fail("Deployment failed"),console.log(`
  ${a.red.bold("\u2716 Deployment Failed!")}`),console.log(`  ${a.dim("Inspect Wrangler output above for error details.")}
`),S&&process.stdout.write(JSON.stringify({ok:false,error:"Deployment failed"})+`
`),process.exit(1);}}),e}var ye=fileURLToPath(import.meta.url),fe=dirname(ye),X="0.1.0";try{X=JSON.parse(readFileSync(join(fe,"../../package.json"),"utf-8")).version;}catch{}var b=new Command;b.name("kyro").description("Kyro CMS - Astro-native headless CMS").version(X);b.command("dev").description("Start Kyro CMS development server").option("-p, --port <port>","Port to run on","4321").option("-h, --host <host>","Host to bind to","localhost").action(async e=>{let{exec:o}=await import('child_process'),t=o(`astro dev --port ${e.port} --host ${e.host}`,{env:{...process.env,NODE_NO_WARNINGS:"1"}});t.stdout?.on("data",r=>process.stdout.write(r)),t.stderr?.on("data",r=>process.stderr.write(r)),process.on("SIGINT",()=>{t.kill("SIGINT"),process.exit(0);}),process.on("SIGTERM",()=>{t.kill("SIGTERM"),process.exit(0);});});b.addCommand(Q());b.addCommand(J());var R=b.command("db").description("Database management commands");R.command("generate").description("Generate migrations from schema").action(async()=>{let{exec:e}=await import('child_process');e("npx drizzle-kit generate",(o,t,r)=>{o&&(console.error("\u274C Generation failed:",o.message),process.exit(1));});});R.command("migrate").description("Run database migrations").action(async()=>{let{exec:e}=await import('child_process');e("npx drizzle-kit migrate",(o,t,r)=>{o&&(console.error("\u274C Migration failed:",o.message),process.exit(1));});});R.command("push").description("Push schema to database (development)").action(async()=>{let{exec:e}=await import('child_process');e("npx drizzle-kit push",(o,t,r)=>{o&&(console.error("\u274C Push failed:",o.message),process.exit(1));});});R.command("seed").description("Seed database with initial data").action(async()=>{let{exec:e}=await import('child_process');e("npx tsx src/database/drizzle/seed.ts",(o,t,r)=>{o&&(console.error("\u274C Seeding failed:",o.message),process.exit(1));});});R.command("studio").description("Open Drizzle Studio").action(async()=>{let{exec:e}=await import('child_process');e("npx drizzle-kit studio",(o,t,r)=>{o&&(console.error("\u274C Studio failed:",o.message),process.exit(1));});});var he=b.command("auth").description("Authentication management commands");he.command("bootstrap").description("Create initial admin user").option("-e, --email <email>","Admin email",process.env.KYRO_ADMIN_EMAIL).option("-p, --password <password>","Admin password",process.env.KYRO_ADMIN_PASSWORD).option("-r, --role <role>","Admin role",process.env.KYRO_ADMIN_ROLE||"admin").action(async e=>{(!e.email||!e.password)&&(console.error("\u274C Email and password are required. Set KYRO_ADMIN_EMAIL and KYRO_ADMIN_PASSWORD env vars or use -e and -p options."),process.exit(1));try{let{bootstrapAdmin:o}=await import('../bootstrap-ROSTPMVL.js'),t=process.env.DATABASE_URL||"",r=t.toLowerCase().startsWith("postgres://")||t.toLowerCase().startsWith("postgresql://"),n=t.toLowerCase().startsWith("mongodb://")||t.toLowerCase().startsWith("mongodb+srv://"),s=!t||t.includes(".db")||t.includes("sqlite")||t.includes("file:"),l;if(r){let{PostgresAuthAdapter:d}=await import('../postgres-auth-adapter-33QOIRXT.js'),{drizzle:p}=await import('drizzle-orm/postgres-js'),{default:h}=await import('postgres'),g=h(t,{max:1,onnotice:()=>{}}),S=p(g);l=new d({db:S});}else if(n){let{MongoDBAuthAdapter:d}=await import('../mongo-auth-adapter-5TJO5J5U.js'),p=await import('mongodb'),h=new p.MongoClient(t);await h.connect();let g=h.db();l=new d({db:g});}else {let{SQLiteAuthAdapter:d}=await import('../sqlite-adapter-UPANATIW.js'),p=process.env.KYRO_AUTH_DB_PATH||"./data/auth.db";l=new d({path:p});}l.connect&&await l.connect();let m=await o({authAdapter:l,adminEmail:e.email,adminPassword:e.password,adminRole:e.role});l.disconnect&&await l.disconnect(),m.success||(console.error("\u274C Failed to create admin:",m.error),process.exit(1));}catch(o){console.error("\u274C Bootstrap failed:",o),process.exit(1);}});b.command("health").description("Check system health").action(async()=>{let e=process.env.DATABASE_URL||"",o=e.toLowerCase().startsWith("postgres://")||e.toLowerCase().startsWith("postgresql://"),t=e.toLowerCase().startsWith("mongodb://")||e.toLowerCase().startsWith("mongodb+srv://"),r=!e||e.includes(".db")||e.includes("sqlite")||e.includes("file:");if(o)try{let{default:n}=await import('postgres'),s=n(e,{max:1,onnotice:()=>{}});await s.unsafe("SELECT 1"),await s.end();}catch{}else if(t)try{let n=await import('mongodb'),s=new n.MongoClient(e);await s.connect(),await s.db().admin().ping(),await s.close();}catch{}else if(r)try{let n=process.env.KYRO_AUTH_DB_PATH||"./data/auth.db",{existsSync:s}=await import('fs');s(n);}catch{}});b.parse();process.argv.slice(2).length||b.outputHelp();